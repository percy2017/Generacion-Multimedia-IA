<!DOCTYPE html>
<html lang="es" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KipuxAI - Iniciar Sesión</title>
    <link rel="icon" href="/public/favicon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilos para el botón de tema */
        .theme-toggle {
            position: fixed;
            top: 15px;
            right: 15px;
            z-index: 1000;
        }

        /* Ajustes de color para el tema oscuro */
        [data-bs-theme="dark"] {
            --bs-body-bg: #212529;
            --bs-body-color: #f8f9fa;
            --bs-card-bg: #343a40;
            --bs-card-border-color: #495057;
            --bs-input-bg: #495057;
            --bs-input-color: #f8f9fa;
            --bs-input-border-color: #6c757d;
        }
        
        /* Ajustes adicionales para tema oscuro */
        [data-bs-theme="dark"] .card {
            background-color: var(--bs-card-bg);
            border-color: var(--bs-card-border-color);
        }
        
        [data-bs-theme="dark"] .form-control {
            background-color: var(--bs-input-bg);
            color: var(--bs-input-color);
            border-color: var(--bs-input-border-color);
        }
        
        [data-bs-theme="dark"] .form-control:focus {
            background-color: var(--bs-input-bg);
            color: var(--bs-input-color);
            border-color: #0d6efd;
            box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
        }
        
        [data-bs-theme="dark"] .form-label {
            color: #e9ecef;
        }
        
        [data-bs-theme="dark"] .btn-primary {
            --bs-btn-bg: #0d6efd;
            --bs-btn-border-color: #0d6efd;
            --bs-btn-hover-bg: #0b5ed7;
            --bs-btn-hover-border-color: #0a58ca;
        }
        
        [data-bs-theme="dark"] .alert-danger {
            --bs-alert-bg: #842029;
            --bs-alert-border-color: #842029;
            --bs-alert-color: #f8d7da;
        }
        
        /* Fondo oscuro para el body en modo oscuro */
        [data-bs-theme="dark"] body {
            background-color: #212529;
        }
    </style>
</head>
<body>
    <!-- Botón de Toggle para Tema -->
    <button id="theme-toggle" class="btn btn-sm btn-outline-secondary theme-toggle" type="button">
        <span id="theme-icon">🌙</span>
    </button>

    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="col-md-6 col-lg-6">
            <div class="card shadow">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <img src="/public/favicon.png" alt="Logo" width="72" height="72" class="mb-3">
                        <h1 class="h3 mb-3 fw-normal">KipuxAI - Iniciar Sesión</h1>
                    </div>

                    <form action="/login" method="post">
                        <div class="mb-3">
                            <label for="litellm_url" class="form-label">URL del Servidor liteLLM</label>
                            <input type="url" class="form-control" id="litellm_url" name="litellm_url" 
                                placeholder="https://..." value="https://litellm.iptvbolivia.com" required>
                        </div>
                        <div class="mb-3">
                            <label for="litellm_key" class="form-label">Clave API de liteLLM</label>
                            <input type="password" class="form-control" id="litellm_key" name="litellm_key" 
                                placeholder="lk-..." required>
                        </div>
                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="remember" name="remember">
                            <label class="form-check-label" for="remember">Recuérdame</label>
                        </div>
                        <button class="w-100 btn btn-lg btn-primary" type="submit">Iniciar Sesión</button>
                    </form>

                    <% if (typeof error !== 'undefined' && error) { %>
                        <div class="alert alert-danger mt-3" role="alert">
                            <%= error %>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Script para Light/Dark Mode -->
    <script src="/public/js/login-theme.js"></script>
</body>
</html>